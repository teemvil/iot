{
	# the name of the sensor
	"name": "RNGsensor",
	
	# the interval time between sending data values, in seconds
	"frequency": 1,
	
	# sensor indentifying code for the MQTT data channel
	"prefix": "sensor1" 
}
