from setuptools import setup

setup(
    name='metropolia-nokia-secure-sensor-management-library',
    version='0.0.1',
    install_requires=[
        'requests',
        'paho-mqtt'
    ],
    packages=['IoTLibrary']
)
